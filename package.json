{
  "name": "cinch",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "add-precommit": "git config --local core.hooksPath .githooks",
    "start": "node server.js",
    "dev": "node --watch server.js public/",
    "lint": "standard",
    "lint:fix": "standard --fix",
    "start:debug": "node --inspect-brk server.js",
    "test": "c8 --reporter=lcov --output-dir=coverage node --test 'test/*.test.js'",
    "test:watch": "node --test --watch test/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "lint-staged": "^17.0.7",
    "sinon": "^22.0.0",
    "standard": "^17.1.2"
  }
}
